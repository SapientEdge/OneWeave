You are GLM-5.2. Reviewing OneWeave (privacy-first Life OS iOS app). Codebase: 56 Swift files, ~15,710 LOC. Built on Linux (no Swift compile), validated by 16 Python mirrors (525+ tests PASS). Stack: Swift 5.9, SwiftUI, SwiftData, iOS 17+.

Below is actual file inventory + key file contents + project context. GROUND EVERY CLAIM IN THESE FILES.

═══════════════════════════════════════════════════════════════
SECTION A — FILE INVENTORY (sorted by LOC, top 30 of 56)
═══════════════════════════════════════════════════════════════
```
997 Sources/OneWeave/OneWeavePrototype.swift        (prototype harness — DEMO/DEBUG)
973 Sources/OneWeave/CompassView.swift               (main tab view)
778 Sources/OneWeave/iOSServiceIntegrations.swift    (Calendar/Health/Contacts/Reminders)
675 Sources/OneWeave/ThreadDetailView.swift          (per-thread UI)
658 Sources/OneWeave/SacredEcho.swift                (AES-256-GCM vault, fail-closed)
639 Sources/OneWeave/PortableExport.swift            (Markdown/OPML/JSON export)
596 Sources/OneWeave/FamilyPod.swift                 (≤6-person pod, reflection-gated)
557 Sources/OneWeave/LifeContext.swift               (core @Model, gamification)
522 Sources/OneWeave/AppLifecycleCoordinator.swift   (encrypted envelope persistence)
494 Sources/OneWeave/CognitiveLoad.swift             (load score, Weave Pause trigger)
464 Sources/OneWeave/DailyBriefings.swift            (morning/evening generation)
419 Sources/OneWeave/OneWeavePrototype+GraphValidation.swift
419 Sources/OneWeave/CareKinThread.swift             (one of 4 threads)
395 Sources/OneWeave/SchemaMigrationPlan.swift       (V1↔V3 versioning)
386 Sources/OneWeave/QuickCaptureInbox.swift         (5-way classifier)
383 Sources/OneWeave/LoomGeometry.swift              (pure-Swift polar layout)
374 Sources/OneWeave/BasicSelfThread.swift           (one of 4 threads)
360 Sources/OneWeave/GraphInsightGenerator.swift     (cross-domain insights)
330 Sources/OneWeave/DecisionLog.swift               (decisions + outcomes)
327 Sources/OneWeave/InvisibleMentor.swift           (local synthesizer)
318 Sources/OneWeave/MentorEchoBridge.swift          (mentor↔echo bridging)
281 Sources/OneWeave/MeaningThread.swift             (one of 4 threads)
268 Sources/OneWeave/P2PWeaveShare.swift             (Network.framework P2P)
258 Sources/OneWeave/RelationshipDecayTracker.swift
252 Sources/OneWeave/ThreadsOverviewView.swift
250 Sources/OneWeave/LivingGraphLoom.swift           (SwiftUI Canvas breathing)
235 Sources/OneWeave/OneWeaveAPI.swift               (public API catalog)
222 Sources/OneWeave/OneWeaveWidgetStubs.swift       (HarmonyWidget, QuestWidget)
195 Sources/OneWeave/DataLeashSettings.swift         (9 integration toggles)
179 Sources/OneWeave/CommandPalette.swift            (universal palette)
177 Sources/OneWeave/LifeGraph.swift                 (entities/relationships)
158 Sources/OneWeave/BodyThreadWeaver.swift          (Health→coherence)
152 Sources/OneWeave/MasteryMapView.swift            (5-tier display)
148 Sources/OneWeave/ResonanceOracle.swift           (decision simulator)
146 Sources/OneWeave/OneWeaveApp.swift               (App entry + modelContainer)
145 Sources/OneWeave/SettingsView.swift              (settings UI)
135 Sources/OneWeave/HistoryView.swift
118 Sources/OneWeave/ResonanceOracleSheet.swift
118 Sources/OneWeave/QuestService.swift              (quest generation/completion)
117 Sources/OneWeave/StewardshipThread.swift
111 Sources/OneWeave/TimelineService.swift
101 Sources/OneWeave/BodyThreadSheet.swift
100 Sources/OneWeave/QuestsView.swift
 94 Sources/OneWeave/DataSeeder.swift
 85 Sources/OneWeave/AppStateMachine.swift           (idle/capturing/weaving/etc.)
 74 Sources/OneWeave/OnboardingView.swift
 64 Sources/OneWeave/LifeGraphiOSIntegrations.swift
 61 Sources/OneWeave/EssenceLedgerView.swift
 60 Sources/OneWeave/OneWeaveSnapshotStore.swift     (widget snapshot)
 54 Sources/OneWeave/InsightGenerator.swift
 44 Sources/OneWeave/WeaveQuest.swift
 40 Sources/OneWeave/TimelineEvent.swift
 33 Sources/OneWeave/StateMachineIndicator.swift
 30 Sources/OneWeave/WeaveSummaryView.swift
 25 Sources/OneWeave/Thread.swift
 25 Sources/OneWeave/MasteryTier.swift               (enumeration)
── Total: 56 files, 15,710 LOC
```

═══════════════════════════════════════════════════════════════
SECTION B — KEY FILE: LifeContext.swift (557 LOC) — CORE @Model
═══════════════════════════════════════════════════════════════
```swift
// Core @Model with gamification state. Public surface used by FamilyPod/QuestService/etc.
// LifeContext holds: essence, level, mastery, harmony, streaks, grace, activeQuests,
// completedQuests, ledger, seasons, isUserReflection gated mutations.

import Foundation
import SwiftData

@Model
public final class LifeContext {
    public var id: UUID = UUID()
    public var createdAt: Date = Date()
    public var lastUpdated: Date = Date()
    public var essence: Int = 0
    public var level: Int = 1
    public var masteryTier: Int = 1
    public var harmonyScore: Double = 0.5
    public var currentStreak: Int = 0
    public var graceAvailable: Int = 2  // restorative grace cap
    public var graceUsedThisWeek: Int = 0
    public var streakStartDate: Date? = nil
    public var currentSeasonName: String = "spring"  // spring/summer/autumn/winter
    public var currentSeasonStart: Date = Date()
    public var reflectionCount: Int = 0
    public var threads: [BasicSelfThread] = []  // 4 thread types
    @Relationship(deleteRule: .cascade) public var ledger: [EssenceEntry] = []
    @Relationship(deleteRule: .cascade) public var activeQuests: [WeaveQuest] = []
    @Relationship(deleteRule: .cascade) public var completedQuests: [WeaveQuest] = []
    public var isUserReflection: Bool = false  // CRITICAL: gates InvisibleMentor synthesis

    // ... constructors, computed properties, etc.
    
    @MainActor
    public func completeQuest(_ quest: WeaveQuest, reflectionText: String) throws {
        guard !reflectionText.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty else {
            throw LifeContextError.emptyReflection
        }
        // CLAUDE-R3-1 fix: reflection gate uses 3 vs 10 essence threshold
        if essence < 3 {
            throw LifeContextError.insufficientEssence
        }
        if essence >= 10 && reflectionText.count < 20 {
            throw LifeContextError.reflectionTooShort
        }
        // ... award essence, push to ledger
    }

    @MainActor
    public func changeSeason(to newSeason: String, reflectionText: String) throws {
        // CLAUDE-R3-2 fix: +20 burst gated behind seasonReflectionCompleted
        guard !reflectionText.isEmpty else { throw LifeContextError.emptyReflection }
        // ... transition logic
    }
}

// errors, helpers omitted for brevity
```

═══════════════════════════════════════════════════════════════
SECTION C — KEY FILE: SacredEcho.swift (658 LOC) — FAIL-CLOSED CRYPTO
═══════════════════════════════════════════════════════════════
```swift
// AES-256-GCM vault. NEMO-R3-018 fix: vaultSeed() now throws (fail-closed).
// No test seed fallback. Deterministic nonce fallback removed.

import Foundation
import CryptoKit
import Security

public enum SacredEchoError: Error {
    case vaultSeedMissing
    case alreadyOpened
    case unlockTooEarly
    case hkdfDerivationFailed
    case envelopeCorrupt
}

public struct SacredEchoAttributes: Codable {
    public let echoID: String
    public let createdAt: Date
    public var unlockAt: Date
    public var opened: Bool = false  // post-fix: opened flag prevents re-open
    public var sealedReflection: Data  // AES-GCM ciphertext
}

@MainActor
public final class SacredEchoVault {
    public static let shared = SacredEchoVault()
    private var keyCache: [String: SymmetricKey] = [:]
    
    public func vaultSeed(for echoID: String) throws -> SymmetricKey {
        // Fail-closed: throws on missing Keychain entry
        let service = "com.oneweave.sacredEcho"
        let account = echoID
        // ... SecItemCopyMatching ... throw SacredEchoError.vaultSeedMissing if not found
    }

    public func seal(_ reflection: String, echoID: String, unlockAt: Date) throws -> SacredEchoAttributes {
        let seed = try vaultSeed(for: echoID)  // throws if missing — no test fallback
        let salt = SHA256.hash(data: Data(echoID.utf8.prefix(8)))  // CLAUDE-R3-9 concern: salt from public echoID
        let key = HKDF<SHA256>.deriveKey(inputKeyMaterial: seed, salt: salt, outputByteCount: 32)
        let sealed = try AES.GCM.seal(reflection.data(using: .utf8)!, using: key)
        return SacredEchoAttributes(echoID: echoID, createdAt: Date(), unlockAt: unlockAt, opened: false, sealedReflection: sealed.combined!)
    }

    public func open(_ attrs: SacredEchoAttributes) throws -> String {
        // Post-fix: blocks re-open
        guard !attrs.opened else { throw SacredEchoError.alreadyOpened }
        // ... decrypt, mark opened=true
    }
}
```

═══════════════════════════════════════════════════════════════
SECTION D — KEY FILE: P2PWeaveShare.swift (268 LOC)
═══════════════════════════════════════════════════════════════
```swift
// Network.framework peer-to-peer. Offline queue. Reflection-gated share.
// BLE/Wi-Fi Aware. Family Pod ≤6. Quiet hours. Cooldown.

import Foundation
import Network

public enum P2PShareError: Error {
    case emptyReflection
    case tooManyInPod
    case quietHours
    case cooldown
    case offlineQueued
}

public actor P2PWeaveShare {
    public static let shared = P2PWeaveShare()
    private var pendingShares: [P2PShare] = []
    private var connection: NWConnection?
    private let cooldownSeconds: TimeInterval = 60
    private var lastShareAt: Date = .distantPast
    
    public func share(_ payload: P2PShare, reflectionText: String) async throws {
        // Reflection gate
        guard !reflectionText.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty else {
            throw P2PShareError.emptyReflection
        }
        // Cooldown
        let since = Date().timeIntervalSince(lastShareAt)
        guard since >= cooldownSeconds else { throw P2PShareError.cooldown }
        lastShareAt = Date()
        // ... try connection, queue if offline
    }
    
    // Family Pod logic delegated to FamilyPod.swift
}
```

═══════════════════════════════════════════════════════════════
SECTION E — KEY FILE: iOSServiceIntegrations.swift (778 LOC)
═══════════════════════════════════════════════════════════════
```swift
// Calendar/EventKit, HealthKit, Contacts, Reminders, Mail, Notes
// All Data-Leash gated. CLAUDE-R3-26 / Grok #18: reordering needed so
// Data Leash check happens BEFORE EventKit permission request.

import Foundation
import EventKit
import HealthKit
import Contacts

public actor iOSServiceIntegrations {
    public static let shared = iOSServiceIntegrations()
    private let eventStore = EKEventStore()
    private let healthStore = HKHealthStore()
    private let contactStore = CNContactStore()
    
    // === CALENDAR ===
    public func importRecentEvents(limit: Int = 20) async throws -> [CalendarEventBinding] {
        // CLAUDE-R3-26 / Grok #18: should check Data Leash BEFORE permission
        // Current order: permission request → import (wrong)
        // Fix order: Data Leash → permission → import
        let granted = try await eventStore.requestFullAccessToEvents()
        guard granted else { throw IntegrationError.permissionDenied }
        // ...
    }
    
    // === HEALTHKIT (Body Thread) ===
    public func importHRV(days: Int = 7) async throws -> [Double] {
        // Reads HRV baseline, sleep fragmentation, RHR via Body Thread
        // Data Leash check + permission + read
    }
    
    // === REMINDERS ===
    public func createReminder(title: String, reflectionText: String) async throws -> ReminderID {
        // CLAUDE-R3-7: createReminder now async; all call sites must await
        guard !reflectionText.isEmpty else { throw IntegrationError.emptyReflection }
        try await eventStore.requestFullAccessToReminders()
        // ...
    }
}
```

═══════════════════════════════════════════════════════════════
SECTION F — KEY FILE: AppLifecycleCoordinator.swift (522 LOC)
═══════════════════════════════════════════════════════════════
```swift
// ScenePhase → encrypted envelope persistence
// App Group: group.com.oneweave
// Snapshot key: "oneweave.snapshot.v1" (NEMO-R3-011: needs bundle-ID scoping)

import Foundation
import SwiftUI
import CryptoKit

public final class AppLifecycleCoordinator: ObservableObject {
    public static let shared = AppLifecycleCoordinator()
    @Published public var sceneState: SceneState = .foreground
    private let appGroupID = "group.com.oneweave"
    private let snapshotKey = "oneweave.snapshot.v1"
    
    public func handleScenePhase(_ phase: ScenePhase) async {
        switch phase {
        case .background:
            await persistEncryptedEnvelope()
        case .active:
            await restoreFromEnvelope()
        default: break
        }
    }
    
    public func persistEncryptedEnvelope() async {
        // Writes to App Group container
        // Uses SacredEcho vault seed as HKDF input
        // NEMO-R3-011: snapshot key not bundle-ID scoped
    }
}
```

═══════════════════════════════════════════════════════════════
SECTION G — KEY FILE: FamilyPod.swift (596 LOC)
═══════════════════════════════════════════════════════════════
```swift
// Family Pod ≤6 members. Quiet hours. Cooldown. Privacy defaults.
// Reflection-gated receive.
// CLAUDE-R3-18/19/20: builder uses non-existent LifeContext members,
// mutates let fields, wrong .title vs .name — DEFERRED for Mac fix.

public struct FamilyPodDigestEntry {
    public let podID: UUID
    public let memberName: String   // should be var not let (R3-20)
    public let sharedEchoID: String?
    public let sharedAt: Date
    public var receivedReflection: String = ""  // should be var (R3-20)
    public let podTitle: String  // wrong: should be .name (R3-20)
}

public actor FamilyPodService {
    public static let shared = FamilyPodService()
    private var pods: [UUID: FamilyPodDigestEntry] = [:]
    public static let maxMembers = 6
    
    public func digest(for lifeContext: LifeContext) throws -> FamilyPodDigestEntry {
        // CLAUDE-R3-18: uses non-existent LifeContext.threads/activeAmplifierName/etc.
        // Mac-side fix needed
    }
}
```

═══════════════════════════════════════════════════════════════
SECTION H — EXISTING VALIDATION SUITES (16, all PASS)
═══════════════════════════════════════════════════════════════
validate_cognitive_load.py
validate_daily_briefings.py
validate_decision_log.py
validate_family_pod.py
validate_mentor_bridge.py
validate_portable_export.py
validate_quick_capture.py
validate_relationship_decay.py
validate_round2_loom.py
validate_schema_migration.py
validate_stress.py
validate_tierA1_cache.py
validate_tierA2_integrations.py
validate_tierA3_echo.py
validate_tierA4_mentor.py
validate_tierA5_lifecycle.py

═══════════════════════════════════════════════════════════════
SECTION I — CONSTITUTION v2.0 (10 principles)
═══════════════════════════════════════════════════════════════
1. One Journey, Not Silos — threads are fluid lenses, not rigid tabs
2. Privacy-First, Zero-Trust — local-only, AES-256-GCM + HKDF fail-closed, no local LLM/Core ML
3. Calm Intelligence — soft animations (0.4 response, 0.7 damping), no streak-shaming
4. Reflection-Gated — non-empty reflectionText for any state-changing action
5. Anti-Addictive — restorative grace (max 2), gentle decay (0.5%/day after 7d), no notifications
6. Genuine Help — solve fragmentation/loneliness/burden/fatigue/meaning
7. Plan Rigorously, Build Real — Spec Kit + Graphify + Python mirrors, no stubs
8-10. (Schema versioning, no-training provider opt-outs, portable data — full text in constitution.md)

═══════════════════════════════════════════════════════════════
SECTION J — KNOWN MAC-SIDE GAPS (14 Nemotron HIGH/blockers, 3 Claude HIGH)
═══════════════════════════════════════════════════════════════
NEMO-R3-001: Info.plist missing
NEMO-R3-002: App Group not provisioned
NEMO-R3-010: Xcode project doesn't exist
NEMO-R3-011: Snapshot key not bundle-ID scoped
NEMO-R3-012: Widget Extension target missing
NEMO-R3-013: AppShortcutsProvider missing
NEMO-R3-014: Apple Developer account setup
NEMO-R3-015/016/017: Echo reviewer artifacts (Decree/Heir)
NEMO-R3-023: V1→V3 migration type change (String → [String:String])
NEMO-R3-024: No migration rollback
NEMO-R3-031: PrivacyPolicy.md underspecified
CLAUDE-R3-18/19/20: FamilyPod API surface
CLAUDE-R3-13: AppShortcutsProvider missing
CLAUDE-R3-25: Widget Extension target

═══════════════════════════════════════════════════════════════
YOUR ANALYSIS (be specific, grounded, max 4000 words)
═══════════════════════════════════════════════════════════════

A. **FEATURE INVENTORY** — number every distinct feature, group by category (Core/Gamif/P2P/Integrations/Cognitive/Foundation), cite file:line

B. **CRITICAL BUGS** — top 10 most important bugs from the code shown, each with file:line + severity + suggested fix

C. **LINUX-SIDE OPPORTUNITIES** — specific Python validators to add, with the algorithm each validates

D. **SPEC KIT T075-T120** — concrete new tasks to add

E. **GLM-5.2 PERSPECTIVE** — unique insights from your training (CRDT, HLC, Sendable discipline, etc.)

F. **PRIORITIZED ACTION PLAN** — single ranked list, 1-25, each: action, file(s), time, depends-on

Use file:line everywhere. No speculation without grounding. Start with "=== GLM-5.2 DEEP REVIEW ===".