You are Claude Code (opus). Review the following for OneWeave, a privacy-first iOS Life OS at /root/hermes-workspace/projects/oneweave.

Read these files FIRST (all are short):
- /root/hermes-workspace/projects/oneweave/ONEWEAVE_HANDOFF_2026-06-27.md
- /root/hermes-workspace/projects/oneweave/.specify/specs/003-production-readiness/tasks.md (last 80 lines — T075+ gaps)

Then review this GLM 5.2 bug list and tell me:
1. Which bugs are REDUNDANT with already-fixed Nemotron/Claude findings (cycle 27 commit 9245b03)?
2. Which are NEW findings worth adding to spec 003 tasks.md as T075-T090?
3. Are any of GLM's recommendations overreach for Linux (need Mac)? Flag with [MAC-NEEDED].
4. Top 5 highest-leverage Linux-fixable items to apply THIS session.

GLM's findings (from review of bundled context):

1. Salt derived from public echoID prefix [SacredEcho.swift:42] — HKDF loses protection when envelope leaks; recommend random 16-byte salt stored in attrs. [HIGH]
2. Permission requested before Data Leash check [iOSServiceIntegrations.swift:21-22] — EventKit calendar import. Constitution #2 violation. [HIGH]
3. Snapshot key not bundle-ID scoped [AppLifecycleCoordinator.swift:14] — `oneweave.snapshot.v1` collision risk. [HIGH]
4. `opened` flag on SacredEchoAttributes Codable struct — re-load from envelope resets flag, re-opening succeeds. [HIGH]
5. completeQuest reflection-length gate skipped when essence 3-10 — any non-empty accepted; inconsistent with essence>=10 branch. [MEDIUM]
6. FamilyPodDigestEntry uses `let` for fields that should be `var`; `podTitle` should be `podName` (R3-20). [MEDIUM]
7. FamilyPodService.digest references non-existent LifeContext.threads/activeAmplifierName (R3-18). [HIGH]
8. P2P cooldown sets lastShareAt BEFORE send succeeds — failed/offline shares consume cooldown. [MEDIUM]
9. SacredEcho keyCache never evicted, never zeroized — unbounded; key material sits in memory. [MEDIUM]
10. AppLifecycleCoordinator async persist from sync-looking ScenePhase handler — risk of iOS background-task kill before flush. [MEDIUM]

GLM also proposed:
- HLC (Hybrid Logical Clocks) for P2P ordering
- CRDT LWW-element-set for offline queue
- LifeContextSnapshot Sendable value type at actor boundaries
- ReflectionGate central policy object
- LRUCache + zeroization for key cache
- Bundle-ID scoped snapshot key
- Random salt + persisted in attrs
- BGProcessingTask for heavy persistence
- 10 new Python validators

Output: numbered, prioritized, with file:line and clear Linux-vs-Mac tagging. Max 1500 words.