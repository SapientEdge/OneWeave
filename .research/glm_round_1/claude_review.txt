I've verified every GLM claim against the actual current source. Several are phantom findings (code GLM described doesn't exist) and several are already-known/already-fixed. Here's the verdict.

---

# GLM 5.2 Review Triage — OneWeave (verified against live source)

Every claim below was checked against the actual files on disk (not GLM's bundled snapshot). Verdicts: ✅ real & present · ⚠️ real but already-known · ❌ phantom (code doesn't match) · 🔧 already-correct.

## 1. REDUNDANT with cycle-27 / known Nemotron+Claude findings

| GLM | Status | Evidence |
|---|---|---|
| **#1 Salt from echoID prefix** ⚠️ | **REDUNDANT** = Claude **R3-9** (known MEDIUM, deferred). Confirmed present: `SacredEcho.swift:274 let salt = Data(echoID.uuidString.prefix(16).utf8)`. Note: HKDF *salt* being public is cryptographically acceptable — the 12-byte **nonce is randomized** (`randomBytes(count:12)`), so envelope leak does **not** break confidentiality. GLM overstates severity (HIGH→really LOW). |
| **#3 Snapshot key not bundle-scoped** ⚠️ | **REDUNDANT** = Nemotron **R3-11** (already logged, Mac). `AppLifecyclePaths.lifeGraphFilename = "oneweave.lifegraph.v1.json"`, appGroup `group.com.oneweave`. |
| **#6 FamilyPodDigestEntry let/podTitle** ⚠️❌ | **REDUNDANT** = Claude **R3-20** (`let`→`var`), but GLM's naming claim is **wrong**: field is `amplifierName` (FamilyPod.swift:209), there is **no `podTitle`**. |
| **#7 digest references non-existent LifeContext members** ✅ | **REDUNDANT** = Claude **R3-18** (known compile-blocker, deferred). Confirmed real: FamilyPod.swift:330/335/338 reference `context.threads`, `context.currentSeasonName`, `context.activeAmplifierName` — none exist (LifeContext has `activeThreads`, `currentSeason`, no amplifier). **This is the one redundant item that's also a genuine compile error worth fixing NOW (see §4).** |

## 2. PHANTOM findings — code does not match GLM's description (drop these)

- **#2 Permission before Data Leash** 🔧 — **FALSE**. Leash *is* checked first: `iOSServiceIntegrations.swift:87 guard leash.isAllowed(.calendar) else { return [] }` precedes `:89 requestAccess()`, with an explicit "Leash-first" comment. No Constitution #2 violation. GLM read stale line numbers.
- **#4 `opened` flag resets on reload** ❌ — **FALSE**. No `SacredEchoAttributes` struct, no resettable `opened` Bool. Re-open guarded by immutable `stateRaw` + `openedAt` (SacredEcho.swift:519-526).
- **#9 keyCache never evicted/zeroized** ❌ — **FALSE**. There is **no keyCache**. Keys derived on-demand per call via `perEchoKey()` (:272-280); nothing accumulates. GLM's LRUCache+zeroization proposal solves a non-problem.

GLM produced 3 confident HIGH/MEDIUM findings for code that isn't there — treat its other claims with corresponding skepticism.

## 3. NEW findings worth adding to tasks.md (T075-T090)

- **T075 [GLM-#5] completeQuest min-length gate** ✅ MEDIUM, Linux-fixable. `LifeContext.swift:455 let bonus = trimmed.isEmpty ? 3 : 10` — the cycle-27 fix corrected the 3-vs-10 essence split, but the non-empty branch accepts a 1-char reflection for full 10 essence, while FamilyPod exit enforces `minExitReflectionChars = 20`. Genuinely inconsistent and new (CLAUDE-R3-1 only covered the essence value, not length).
- **T076 [GLM-#10] Background persist unguarded** ✅ MEDIUM, **[MAC-NEEDED]**. `AppLifecycleCoordinator.applicationDidEnterBackground` (:393-418) does synchronous `mc.save()` + `persist()` in a bare handler with **no `beginBackgroundTask`/`BGTaskScheduler`** assertion → iOS can kill before flush. Real, but the fix (`UIApplication.beginBackgroundTask` / `BGProcessingTaskRequest`) is iOS-only API — uncompilable/unverifiable on Linux.
- **T077 [GLM-#8] P2P cooldown timing** — **UNVERIFIABLE / likely phantom**. No `lastShareAt` field exists anywhere; `P2PWeaveShare` share path (`queueShare`/`sendViaWebRTC`) sets no cooldown, and `FamilyPodPolicy.nextEligiblePublish` only *reads*. Add as a **spec note to verify**, not a fix.
- **T078 ReflectionGate central policy** (GLM proposal) — worthwhile: consolidates T075 + the scattered 3/10/20-char rules into one testable object. Linux-fixable, good Constitution-#2 hardening.

GLM's other proposals — **HLC, CRDT LWW-set, LifeContextSnapshot Sendable** — are speculative architecture for P2P/concurrency that isn't load-bearing yet. Log as **T085-T090 "deferred design spikes,"** not production tasks. The Sendable/actor-boundary work is **[MAC-NEEDED]** (needs the compiler's concurrency checker to mean anything).

## 4. Top 5 highest-leverage Linux-fixable items — THIS session

1. **Fix R3-18 / GLM-#7 compile blocker** (FamilyPod.swift:330/335/338). Add three computed properties to `LifeContext` — `var threads`/use `activeThreads`, `var currentSeasonName { currentSeason.displayName }`, `var activeAmplifierName: String?`. The handoff deferred this to Mac as "needs API surface additions," but the *edit* is pure Swift and removes a hard compile error — write it now, let Mac `xcodebuild` confirm. **Highest leverage: it's blocking the build.**
2. **T075 completeQuest min-length** (LifeContext.swift:455). Gate the full-10 award behind `trimmed.count >= 20` (or central ReflectionGate), falling back to partial essence below threshold. Pure logic, has a Python mirror to validate.
3. **GLM-#1 / R3-9 salt hardening** (SacredEcho.swift:274). Low real severity, but cheap: generate a random 16-byte salt at seal time, persist in `attributes["salt"]`, read it back in `open()`. Self-contained, Linux-codeable, closes the known finding. (Keep nonce as-is.)
4. **Snapshot/filename namespacing** (AppLifecyclePaths). Partial-fix on Linux: route the literal through a single `bundlePrefix` constant so the Mac side only edits one place. **True bundle-ID scoping is [MAC-NEEDED]** but the refactor de-risks it now.
5. **T078 ReflectionGate policy object.** Extract the 3/10/20-char rules into one struct with unit tests — pays down #2 and the inconsistency #5 surfaced, and gives the Mac coworker a single audit point for Constitution #2.

Then: re-run `bash .research/validate_all.sh` and append T075-T078 to `tasks.md` (file currently ends at T074 / Phase P1).

---

**Bottom line:** Of GLM's 10 findings — **3 phantom** (#2/#4/#9), **4 redundant** (#1/#3/#6/#7), **2 genuinely new** (#5, #10), **1 unverifiable** (#8). Net new actionable Linux work ≈ 2 items (#5, #7-fix) plus the ReflectionGate refactor. GLM's HLC/CRDT/Sendable proposals are premature; its crypto severity ratings are inflated (public HKDF salt ≠ broken). Items 1-3 and 5 in §4 are Linux-fixable today; #10 and true bundle scoping are **[MAC-NEEDED]**.

Want me to apply §4 items 1-3 and 5 now and append T075-T078 to tasks.md?
